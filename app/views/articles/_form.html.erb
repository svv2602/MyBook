<% if @article.errors.any? %>
  <ul>
    <%= @article.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
  </ul>
<% end %>
<%# @divisions = Division.all %>

<% value_array = [] %>
<% Division.all.each do |c| %>
  <% arr = [] %>
  <% arr << c.name << c.id %>
  <% value_array << arr %>
<% end %>



<%= form_with model: @article do |f| %>
  <div>
    <%= f.select :division_id, options_for_select(value_array, f.object.division_id || default_value) %>
  </div>

  <div>
    <%= f.label :title %>
  </div>
  <div>
    <%= f.text_field :title %>
  </div>
  <div>
    <%= f.label :body %>
  </div>
  <div>
    <%= f.cktext_area :body, rows: 10 %>
  </div>
  <br>
  <div>
    <%= f.submit 'Сохранить изменения' %>
  </div>
<% end %>
<br>
